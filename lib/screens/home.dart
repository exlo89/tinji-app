import 'package:flutter/material.dart';

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    print('hjoojjo0');
    return Scaffold(
      appBar: AppBar(
        title: Text('Tinji'),
      ),
      body: Center(
        child: Text('jjooooooo'),
      ),
    );
  }
}
